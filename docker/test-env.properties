# More members than partitions (4 replicas x 2 concurrency = 8 members, 6 partitions):
# the load-aware group must still spread traffic evenly across the 4 instances.
PARTITIONS_COUNT=6
LISTENER_CONCURRENCY=2
